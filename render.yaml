services:
  - type: web
    name: chatbot-app
    env: python
    region: oregon
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app_web.py"
    plan: free
    autoDeploy: true
    envVars:
      - key: PORT
        value: 10000
